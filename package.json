{
  "name": "resume-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Editable resume page with SQLite persistence and avatar upload.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
